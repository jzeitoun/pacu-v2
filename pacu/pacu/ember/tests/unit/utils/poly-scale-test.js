import polyScale from 'pacu-v2/utils/poly-scale';
import { module, test } from 'qunit';

module('Unit | Utility | poly scale');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = polyScale();
  assert.ok(result);
});
